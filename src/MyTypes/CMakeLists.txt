set(PART_NAME MyTypes)
set(SOURCE_FILES ${SOURCE_FILES} ${PART_NAME}/${PART_NAME}.cpp PARENT_SCOPE)
set(INCLUDE_DIRECTORIES ${INCLUDE_DIRECTORIES} "${PROJECT_SOURCE_DIR}/${SOURCE_DIR_NAME}/${PART_NAME}" PARENT_SCOPE)
#file(COPY_FILE ${PART_NAME}.h "${BINARY_INCLUDE}/${PART_NAME}.h" ONLY_IF_DIFFERENT)